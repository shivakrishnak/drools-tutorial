package drools.rules

import org.drools.Product;
import org.drools.Product.Category;

rule "Classify Product - Low Range"
    when
        $i: Product(cost < 200)
    then
        $i.setCategory(Category.LOW_RANGE);
end


rule "Classify Product - Mid Range"
    when
        $i: Product(cost > 500)
    then
        $i.setCategory(Category.MID_RANGE);
end

